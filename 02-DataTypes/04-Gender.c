#include <stdio.h>
#include <stdlib.h>

/*Declare two string (char array) variables holding your first name and last name. Print them in the console (mind
adding an interval between them).
 */
int main(int argc, char** argv)
{
    int isFemale = 0; // false
 
    if (isFemale) //  every think is true without zero 0
    {
        printf("true");
    }
    else if (!isFemale)
    {
        printf("false");
    }
    //printf("%c\n", a);

    return 0;
}

