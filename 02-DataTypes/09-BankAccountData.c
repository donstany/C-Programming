#include <stdio.h>
#include <stdlib.h>

/*
A bank account has a holder name (first name, middle name and last name), available amount of money (balance),
bank name, IBAN and 3 credit card numbers associated with the account. Declare the variables needed to keep the
information for a single bank account using the appropriate data types and descriptive names.
 */
int main(int argc, char** argv) {

    char holderName[30] = "";
    //  scanf("%29s",holderName); // limit input char  
    //  printf("%s",holderName);
    double amountOfMoney = 0.0;
    char bankName[35]="";
    long long iban = 0.0;
    long long creditCardNumber1 = 0.0;
    long long creditCardNumber2 = 0.0;
    long long creditCardNumber3 = 0.0;

    return (EXIT_SUCCESS);
}

